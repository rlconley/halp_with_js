<h3><%= link_to 'HALP', root_path %> |
<% if logged_in? %>
  Logged in as <%= current_user.name %> |
  <%= link_to 'Log out', login_path, :method => :delete %>
<% else %>
  <%= link_to 'Log in', new_login_path %>
<% end %>
</h3>